<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:updatePeriodMillis="1800000"
    android:previewImage="@drawable/xw"
    android:initialLayout="@layout/xian_price_widget"
    android:description="@string/xian_price_widget_description"
    android:resizeMode="horizontal"
    android:widgetCategory="home_screen">
    <!-- Set grid to 2x1:
         minWidth = 70dp * 2 - 30dp = 110dp
         minHeight = 70dp * 1 - 30dp = 40dp -->
    <!-- Update every 30 minutes -->
    <!-- Use existing drawable as placeholder preview -->
    <!-- Description defined in strings.xml -->
</appwidget-provider>
